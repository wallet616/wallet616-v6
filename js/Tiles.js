/*
var set_size = function() {
    $(".tile").each(function() {
        var width = $(this).width();
        var height = 0.4 * width;
        $(this).css("height", height + "px");
    });
};

$(window).ready(() => { setTimeout(set_size, 50); });
$(window).resize(set_size);
*/